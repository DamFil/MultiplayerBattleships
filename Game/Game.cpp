/*#include "NewPlayer.h"

int main()
{
    NewPlayer *p1 = new NewPlayer();
    NewPlayer *p2 = new NewPlayer();

    p1->showMap();

    p1->newShip(A, 'A', 2, 'H');
    p1->newShip(B, 'C', 6, 'V');
    p1->newShip(C, 'F', 3, 'H');
    p1->newShip(D, 'D', 7, 'V');
    p1->newShip(D, 'A', 10, 'H');
    p1->newShip(S, 'J', 10, 'V');
    p1->newShip(S, 'I', 9, 'V');

    p2->attack(p1, 'A', 2);
    p2->attack(p1, 'E', 4);
    p2->attack(p1, 'G', 3);

    // p->newShip(A, 'F', 1, 'H');
    // p->newShip(B, 'C', 1, 'H');
    // p->newShip(C, 'H', 4, 'H');
    // p->newShip(D, 'A', 2, 'V');
    // p->newShip(D, 'C', 6, 'H');
    // p->newShip(S, 'F', 7, 'V');
    // p->newShip(S, 'C', 5, 'V');

    p1->showMap();
    cout << "~~~~~~~~~~~~~~~~~~~~~~~ATTEMPTS~~~~~~~~~~~~~~~~~~~~~~~" << endl;
    p1->showAttemptsMap();

    return 0;
}*/